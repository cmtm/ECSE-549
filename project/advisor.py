import device




def main(argv):
	# initialize

	# present gui

	# hooks (events) in gui trigger calls to functions
	# that traverse interface tree if needed

	# the event of the button being pushed triggers a call to the
	# sanity checker.
	# the sanity checker returns pass or an error report
	
	# upon passing the sanity checks, the param and associated goodness
	# functions get passed to the solver
	# 
